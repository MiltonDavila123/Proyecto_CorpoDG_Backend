python -m django --version ---> Ver la version
python -m django startproject corpodg . ----------> Para que inicie el startproject corpodg en la misma carpeta
python manage.py startapp servicios --------------> Para iniciar una app
python manage.py runserver ---------> para que inicie el servidor
